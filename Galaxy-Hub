--// Services
local Players = game:GetService("Players")
local LocalPlayer = Players.LocalPlayer
local UIS = game:GetService("UserInputService")
local RunService = game:GetService("RunService")
local VirtualUser = game:GetService("VirtualUser")

--// ScreenGui
local ScreenGui = Instance.new("ScreenGui")
ScreenGui.Name = "Tak_UI"
ScreenGui.ResetOnSpawn = false
ScreenGui.Parent = game:GetService("CoreGui")

--// Toggle Button
local ToggleButton = Instance.new("TextButton")
ToggleButton.Size = UDim2.new(0, 100, 0, 40)
ToggleButton.Position = UDim2.new(0, 20, 0.5, -20)
ToggleButton.BackgroundColor3 = Color3.fromRGB(50, 50, 80)
ToggleButton.Text = "📂 เปิดเมนู"
ToggleButton.TextColor3 = Color3.fromRGB(255,255,255)
ToggleButton.Font = Enum.Font.GothamBold
ToggleButton.TextSize = 14
ToggleButton.Parent = ScreenGui

--// Main Frame
local MainFrame = Instance.new("Frame")
MainFrame.Size = UDim2.new(0, 550, 0, 320)
MainFrame.Position = UDim2.new(0.5, -275, 0.5, -160)
MainFrame.BackgroundColor3 = Color3.fromRGB(25, 25, 40)
MainFrame.BorderSizePixel = 0
MainFrame.Visible = false
MainFrame.Parent = ScreenGui

local UICorner = Instance.new("UICorner")
UICorner.CornerRadius = UDim.new(0, 12)
UICorner.Parent = MainFrame

-- Title Bar
local TitleBar = Instance.new("TextLabel")
TitleBar.Size = UDim2.new(1, 0, 0, 40)
TitleBar.BackgroundColor3 = Color3.fromRGB(35, 35, 60)
TitleBar.Text = "🌌 Galaxy Hub"
TitleBar.TextColor3 = Color3.fromRGB(200, 200, 255)
TitleBar.Font = Enum.Font.GothamBold
TitleBar.TextSize = 18
TitleBar.Parent = MainFrame

-- Dragging System
local dragging, dragInput, dragStart, startPos
MainFrame.InputBegan:Connect(function(input)
    if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
        dragging = true
        dragStart = input.Position
        startPos = MainFrame.Position
        input.Changed:Connect(function()
            if input.UserInputState == Enum.UserInputState.End then
                dragging = false
            end
        end)
    end
end)

MainFrame.InputChanged:Connect(function(input)
    if input.UserInputType == Enum.UserInputType.MouseMovement or input.UserInputType == Enum.UserInputType.Touch then
        dragInput = input
    end
end)

UIS.InputChanged:Connect(function(input)
    if input == dragInput and dragging then
        local delta = input.Position - dragStart
        MainFrame.Position = UDim2.new(startPos.X.Scale, startPos.X.Offset + delta.X,
        startPos.Y.Scale, startPos.Y.Offset + delta.Y)
    end
end)

-- Toggle Button Function
local isOpen = false
ToggleButton.MouseButton1Click:Connect(function()
    isOpen = not isOpen
    MainFrame.Visible = isOpen
    ToggleButton.Text = isOpen and "❌ ปิดเมนู" or "📂 เปิดเมนู"
end)

-- Sidebar Tabs
local Tabs = Instance.new("Frame")
Tabs.Size = UDim2.new(0, 150, 1, -40)
Tabs.Position = UDim2.new(0, 0, 0, 40)
Tabs.BackgroundColor3 = Color3.fromRGB(30, 30, 50)
Tabs.BorderSizePixel = 0
Tabs.Parent = MainFrame

local TabLayout = Instance.new("UIListLayout")
TabLayout.SortOrder = Enum.SortOrder.LayoutOrder
TabLayout.Padding = UDim.new(0, 5)
TabLayout.Parent = Tabs

-- Content Frame
local ContentFrame = Instance.new("Frame")
ContentFrame.Size = UDim2.new(1, -150, 1, -40)
ContentFrame.Position = UDim2.new(0, 150, 0, 40)
ContentFrame.BackgroundColor3 = Color3.fromRGB(40, 40, 70)
ContentFrame.BorderSizePixel = 0
ContentFrame.Parent = MainFrame

-- Function to create Tabs
local function CreateTab(name)
    local Button = Instance.new("TextButton")
    Button.Size = UDim2.new(1, -10, 0, 35)
    Button.BackgroundColor3 = Color3.fromRGB(55, 55, 75)
    Button.Text = name
    Button.TextColor3 = Color3.fromRGB(255,255,255)
    Button.Font = Enum.Font.GothamBold
    Button.TextSize = 14
    Button.Parent = Tabs

    local TabFrame = Instance.new("Frame")
    TabFrame.Size = UDim2.new(1, 0, 1, 0)
    TabFrame.BackgroundTransparency = 1
    TabFrame.Visible = false
    TabFrame.Parent = ContentFrame

    Button.MouseButton1Click:Connect(function()
        for _, frame in pairs(ContentFrame:GetChildren()) do
            if frame:IsA("Frame") then
                frame.Visible = false
            end
        end
        TabFrame.Visible = true
    end)

    return TabFrame
end

-- Create Tabs
local MainTab = CreateTab("Main")
local PlayerTab = CreateTab("Player")
local ESPTab = CreateTab("ESP & การมองเห็น")
local MoveTab = CreateTab("การเคลื่อนที่")
local SettingsTab = CreateTab("Settings")

------------------------------------------------------
-- Main Tab
local Label = Instance.new("TextLabel")
Label.Size = UDim2.new(1, 0, 0, 30)
Label.Text = "✨ Welcome to Galaxy Hub!"
Label.TextColor3 = Color3.fromRGB(255,255,255)
Label.BackgroundTransparency = 1
Label.Font = Enum.Font.GothamBold
Label.TextSize = 18
Label.Parent = MainTab

-- Discord Button
local DiscordBtn = Instance.new("TextButton")
DiscordBtn.Size = UDim2.new(1, -20, 0, 30)
DiscordBtn.Position = UDim2.new(0, 10, 0, 40)
DiscordBtn.BackgroundColor3 = Color3.fromRGB(70, 70, 100)
DiscordBtn.Text = "🌌 เข้าร่วม Discord"
DiscordBtn.TextColor3 = Color3.fromRGB(255,255,255)
DiscordBtn.Font = Enum.Font.GothamBold
DiscordBtn.TextSize = 14
DiscordBtn.Parent = MainTab
DiscordBtn.MouseButton1Click:Connect(function()
    setclipboard("https://discord.gg/9mAAGXwpsv")
end)

-- Facebook Button
local FBBtn = Instance.new("TextButton")
FBBtn.Size = UDim2.new(1, -20, 0, 30)
FBBtn.Position = UDim2.new(0, 10, 0, 75)
FBBtn.BackgroundColor3 = Color3.fromRGB(70, 70, 100)
FBBtn.Text = "👥 เข้ากลุ่ม Facebook"
FBBtn.TextColor3 = Color3.fromRGB(255,255,255)
FBBtn.Font = Enum.Font.GothamBold
FBBtn.TextSize = 14
FBBtn.Parent = MainTab
FBBtn.MouseButton1Click:Connect(function()
    setclipboard("https://facebook.com/groups/1293681054338840/")
end)

-- DateTime
local DateTimeLabel = Instance.new("TextLabel")
DateTimeLabel.Size = UDim2.new(1, -20, 0, 30)
DateTimeLabel.Position = UDim2.new(0, 10, 0, 110)
DateTimeLabel.BackgroundTransparency = 1
DateTimeLabel.TextColor3 = Color3.fromRGB(200,200,255)
DateTimeLabel.Font = Enum.Font.Gotham
DateTimeLabel.TextSize = 14
DateTimeLabel.Parent = MainTab

task.spawn(function()
    while true do
        DateTimeLabel.Text = "⏰ " .. os.date("%d/%m/%Y %H:%M:%S")
        task.wait(1)
    end
end)

-- FPS Counter
local FPSLabel = Instance.new("TextLabel")
FPSLabel.Size = UDim2.new(1, -20, 0, 30)
FPSLabel.Position = UDim2.new(0, 10, 0, 140)
FPSLabel.BackgroundTransparency = 1
FPSLabel.TextColor3 = Color3.fromRGB(150,255,150)
FPSLabel.Font = Enum.Font.Gotham
FPSLabel.TextSize = 14
FPSLabel.Parent = MainTab

local lastUpdate, frames = tick(), 0
RunService.RenderStepped:Connect(function()
    frames += 1
    if tick() - lastUpdate >= 1 then
        FPSLabel.Text = "📊 FPS: " .. frames
        frames = 0
        lastUpdate = tick()
    end
end)

-- FPS Button
local FPSBtn = Instance.new("TextButton")
FPSBtn.Size = UDim2.new(1, -20, 0, 30)
FPSBtn.Position = UDim2.new(0, 10, 0, 170)
FPSBtn.BackgroundColor3 = Color3.fromRGB(70, 70, 100)
FPSBtn.Text = "⚡ ตั้งค่า FPS (999)"
FPSBtn.TextColor3 = Color3.fromRGB(255,255,255)
FPSBtn.Font = Enum.Font.GothamBold
FPSBtn.TextSize = 14
FPSBtn.Parent = MainTab
FPSBtn.MouseButton1Click:Connect(function()
    setfpscap(999)
end)

-- Graphic Button
local GraphicBtn = Instance.new("TextButton")
GraphicBtn.Size = UDim2.new(1, -20, 0, 30)
GraphicBtn.Position = UDim2.new(0, 10, 0, 205)
GraphicBtn.BackgroundColor3 = Color3.fromRGB(70, 70, 100)
GraphicBtn.Text = "🌀 ลบหมอก แสง เงา"
GraphicBtn.TextColor3 = Color3.fromRGB(255,255,255)
GraphicBtn.Font = Enum.Font.GothamBold
GraphicBtn.TextSize = 14
GraphicBtn.Parent = MainTab
GraphicBtn.MouseButton1Click:Connect(function()
    game.Lighting.FogEnd = 1e5
    game.Lighting.GlobalShadows = false
    game.Lighting.Brightness = 2
end)

------------------------------------------------------
-- Player Tab
local WalkLabel = Instance.new("TextLabel")
WalkLabel.Size = UDim2.new(1, -20, 0, 30)
WalkLabel.Position = UDim2.new(0, 10, 0, 20)
WalkLabel.BackgroundTransparency = 1
WalkLabel.TextColor3 = Color3.fromRGB(255,255,255)
WalkLabel.Text = "🚶 ปรับความเร็วเดิน"
WalkLabel.Parent = PlayerTab

local WalkBox = Instance.new("TextBox")
WalkBox.Size = UDim2.new(1, -20, 0, 30)
WalkBox.Position = UDim2.new(0, 10, 0, 55)
WalkBox.BackgroundColor3 = Color3.fromRGB(70, 70, 100)
WalkBox.Text = "16"
WalkBox.TextColor3 = Color3.new(1,1,1)
WalkBox.Parent = PlayerTab
WalkBox.FocusLost:Connect(function()
    local val = tonumber(WalkBox.Text)
    if val and LocalPlayer.Character and LocalPlayer.Character:FindFirstChild("Humanoid") then
        LocalPlayer.Character.Humanoid.WalkSpeed = val
    end
end)

local JumpLabel = Instance.new("TextLabel")
JumpLabel.Size = UDim2.new(1, -20, 0, 30)
JumpLabel.Position = UDim2.new(0, 10, 0, 95)
JumpLabel.BackgroundTransparency = 1
JumpLabel.TextColor3 = Color3.fromRGB(255,255,255)
JumpLabel.Text = "🦘 ปรับความสูงกระโดด"
JumpLabel.Parent = PlayerTab

local JumpBox = Instance.new("TextBox")
JumpBox.Size = UDim2.new(1, -20, 0, 30)
JumpBox.Position = UDim2.new(0, 10, 0, 130)
JumpBox.BackgroundColor3 = Color3.fromRGB(70, 70, 100)
JumpBox.Text = "50"
JumpBox.TextColor3 = Color3.new(1,1,1)
JumpBox.Parent = PlayerTab
JumpBox.FocusLost:Connect(function()
    local val = tonumber(JumpBox.Text)
    if val and LocalPlayer.Character and LocalPlayer.Character:FindFirstChild("Humanoid") then
        LocalPlayer.Character.Humanoid.JumpPower = val
    end
end)

------------------------------------------------------
-- ESP & การมองเห็น
local function CreateESPButton(name, url, posY)
    local Btn = Instance.new("TextButton")
    Btn.Size = UDim2.new(1, -20, 0, 30)
    Btn.Position = UDim2.new(0, 10, 0, posY)
    Btn.BackgroundColor3 = Color3.fromRGB(70, 70, 100)
    Btn.Text = name
    Btn.TextColor3 = Color3.new(1,1,1)
    Btn.Font = Enum.Font.GothamBold
    Btn.TextSize = 14
    Btn.Parent = ESPTab
    Btn.MouseButton1Click:Connect(function()
        loadstring(game:HttpGet(url))()
    end)
end

CreateESPButton("🔍 ESP Players", "https://pastebin.com/raw/ZkLhNuDL", 20)
CreateESPButton("🤖 ESP BOT,NPC", "https://pastebin.com/raw/q26QuUBF", 55)

------------------------------------------------------
-- การเคลื่อนที่
local function CreateMoveButton(name, url, posY)
    local Btn = Instance.new("TextButton")
    Btn.Size = UDim2.new(1, -20, 0, 30)
    Btn.Position = UDim2.new(0, 10, 0, posY)
    Btn.BackgroundColor3 = Color3.fromRGB(70, 70, 100)
    Btn.Text = name
    Btn.TextColor3 = Color3.new(1,1,1)
    Btn.Font = Enum.Font.GothamBold
    Btn.TextSize = 14
    Btn.Parent = MoveTab
    Btn.MouseButton1Click:Connect(function()
        loadstring(game:HttpGet(url))()
    end)
end

CreateMoveButton("🚀 บิน V1", "https://pastebin.com/raw/iUVERBrs", 20)
CreateMoveButton("🔥 วาป", "https://pastebin.com/raw/K5FYvtvN", 55)
CreateMoveButton("🦴 ทะลุกำแพง", "https://pastebin.com/raw/7Qab6kg9", 90)
CreateMoveButton("🏃 วิ่งเร็ว 4 เท่า", "https://pastebin.com/raw/pZhkm5mD", 125)

------------------------------------------------------
-- Settings Tab
local ColorBtn = Instance.new("TextButton")
ColorBtn.Size = UDim2.new(1, -20, 0, 35)
ColorBtn.Position = UDim2.new(0, 10, 0, 40)
ColorBtn.BackgroundColor3 = Color3.fromRGB(100, 70, 120)
ColorBtn.Text = "🎨 เปลี่ยนสี UI (สุ่ม)"
ColorBtn.TextColor3 = Color3.new(1,1,1)
ColorBtn.Parent = SettingsTab
ColorBtn.MouseButton1Click:Connect(function()
    MainFrame.BackgroundColor3 = Color3.fromRGB(math.random(20,200), math.random(20,200), math.random(20,200))
end)

local ResetBtn = Instance.new("TextButton")
ResetBtn.Size = UDim2.new(1, -20, 0, 35)
ResetBtn.Position = UDim2.new(0, 10, 0, 85)
ResetBtn.BackgroundColor3 = Color3.fromRGB(200, 70, 70)
ResetBtn.Text = "🔄 รีเซ็ต UI"
ResetBtn.TextColor3 = Color3.new(1,1,1)
ResetBtn.Parent = SettingsTab
ResetBtn.MouseButton1Click:Connect(function()
    MainFrame:Destroy()
    ScreenGui:Destroy()
end)

-- Default show Main Tab
MainTab.Visible = true

------------------------------------------------------
-- Anti AFK
Players.LocalPlayer.Idled:Connect(function()
    VirtualUser:CaptureController()
    VirtualUser:ClickButton2(Vector2.new())
end)
